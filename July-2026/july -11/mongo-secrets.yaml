# before deployment we should apply this file for to refrence it int the deployment file.
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
type: Opaque
data:
  mongo-root-username: dXNlcm5hbWU=
  #echo -n <text> | base64
  #eg: echo -n 'username' | base64
  mongo-root-password: cGFzc3dvcmQ=
  #echo -n <text> | base64
  #eg : echo -n 'password' | base64
